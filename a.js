//variables

//vey rarely will you use this
var nameOfVariable = ''

//once set will never change
//only matters in the scope of the function
const constantVariable = 'constant'

//changeable variable; types/ values
let changeableVariable = 'something that\'s going to change'

console.log(changeableVariable)
changeableVariable = 'something'
console.log(changeableVariable)


//arrays
const coolArray =[
    'string',
    0,
    {},
    0.4
]

//will make the thing inside the array change but the actual array and type will never change
console.log(coolArray(4))
coolArray[0] = 'another string'
//push, filter, index

//Numbers
123
//floating points
.23
